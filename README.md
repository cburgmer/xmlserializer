XMLSerializer.js serializes a DOM subtree or DOM document into XML/XHTML.

Example
-------

For a browser based example run `./go` and see `example.html`.

Run tests and build for the browser
-----------------------------------

    $ ./go
